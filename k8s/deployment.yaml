apiVersion: v1
kind: Service
metadata:
  namespace: zodiac
  name: orion-ui
  labels:
    app: orion-ui
spec:
  type: NodePort
  selector:
    app: orion-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: zodiac
  name: orion-ui
  labels:
    app: orion-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orion-ui
  template:
    metadata:
      labels:
        app: orion-ui
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: orion-ui
          image: git.tu-berlin.de:5000/zodiac/zodiac-meta/orion-ui@sha256:743638d3df9ac6d72704c77b23ef32c14df4889bbe0ac759906fda652d71931e
          env:
          - name: VITE_CHAT_API_BASE
            value: "mcp-client-service:8001"
          - name: VITE_CHAT_API_PATH
            value: "/chat"
          - name: VITE_AGENT_API_BASE
            value: http://agent-api:3000
          ports:
            - containerPort: 80

